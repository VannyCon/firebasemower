specVersion: "v1beta"
serviceId: "firebasemower"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebasemower-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
